[toolchain]
channel = "1.96.0"
components = ["clippy", "rustfmt"]
# aarch64-unknown-linux-gnu is a release/cross-build target; pin it here so the
# pinned toolchain (not just the CI action's `stable`) has the target installed.
targets = ["aarch64-unknown-linux-gnu"]
