# --- cfn-lint-test ---
# rule: E4002
# description: Metadata section validation - must be object, no null values
# expect:
#   - rule: E4002
#     path: /Metadata/NullEntry
#     message_contains: "is not of type"
# ---
Metadata:
  ValidKey: some-string-value
  NullEntry: null

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: test-bucket
