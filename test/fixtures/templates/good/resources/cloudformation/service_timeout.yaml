Resources:
  CustomResource:
    Type: AWS::CloudFormation::CustomResource
    Properties:
      ServiceToken: "arn::aws::fake"
      ServiceTimeout: 60

  CustomResource2:
    Type: Custom::CustomResource
    Properties:
      ServiceToken: "arn::aws::fake"
      ServiceTimeout: 90
