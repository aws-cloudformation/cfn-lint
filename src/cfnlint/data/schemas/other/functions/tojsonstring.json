{
 "cfnContext": {
  "functions": [
   "Fn::FindInMap",
   "Fn::GetAtt",
   "Fn::GetAZs",
   "Fn::If",
   "Fn::Select",
   "Fn::Split",
   "Ref"
  ],
  "pseudoParameters": [
   "AWS::AccountId",
   "AWS::Partition",
   "AWS::Region",
   "AWS::StackId",
   "AWS::StackName",
   "AWS::URLSuffix",
   "AWS::NoValue"
  ],
  "schema": {
   "minItems": 1,
   "minProperties": 1,
   "type": [
    "array",
    "object"
   ]
  }
 }
}
