AWSTemplateFormatVersion: '2010-09-09'
Resources:
  # E3680: Application LB with only 1 subnet (requires at least 2)
  ALBOneSubnet:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Type: application
      Subnets:
        - subnet-11111111
