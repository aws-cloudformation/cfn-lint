AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::LanguageExtensions
Resources:
  Fn::ForEach::BucketLoop:
    - Identifier
    - - prod
      - staging
      - dev
    - ${Identifier}Bucket:
        Type: AWS::S3::Bucket
        Properties:
          BucketName: !Sub '${Identifier}-data'
