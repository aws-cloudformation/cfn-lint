AWSTemplateFormatVersion: '2010-09-09'
Description: This is a valid but somewhat long description that tests E1003. The actual limit is 1024 characters so this is fine.
InvalidTopLevelKey: should-not-be-here
Resources:
  Bucket:
    Type: AWS::S3::Bucket
