{
 "additionalIdentifiers": [
  [
   "/properties/PromptRouterId"
  ]
 ],
 "additionalProperties": false,
 "definitions": {
  "PromptRouterTargetModel": {
   "additionalProperties": false,
   "description": "A target model for the prompt router.",
   "properties": {
    "ModelArn": {
     "description": "The target model's ARN.",
     "maxLength": 2048,
     "minLength": 1,
     "pattern": "(^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}::foundation-model/[a-z0-9-]{1,63}[.]{1}([a-z0-9-]{1,63}[.]){0,2}[a-z0-9-]{1,63}([:][a-z0-9-]{1,63}){0,2})|(^arn:aws(|-us-gov|-cn|-iso|-iso-b):bedrock:(|[0-9a-z-]{0,20}):(|[0-9]{12}):(inference-profile|application-inference-profile)/[a-zA-Z0-9-:.]+)$",
     "type": "string"
    }
   },
   "required": [
    "ModelArn"
   ],
   "type": "object"
  },
  "RoutingCriteria": {
   "additionalProperties": false,
   "description": "Routing criteria for a prompt router.",
   "properties": {
    "ResponseQualityDifference": {
     "description": "The response quality difference threshold for routing.",
     "maximum": 100,
     "minimum": 0,
     "type": "number"
    }
   },
   "required": [
    "ResponseQualityDifference"
   ],
   "type": "object"
  }
 },
 "description": "Definition of AWS::Bedrock::DefaultPromptRouter Resource Type",
 "handlers": {
  "list": {
   "permissions": [
    "bedrock:ListPromptRouters"
   ]
  },
  "read": {
   "permissions": [
    "bedrock:GetPromptRouter"
   ]
  }
 },
 "primaryIdentifier": [
  "/properties/PromptRouterArn"
 ],
 "properties": {
  "CreatedAt": {
   "description": "The timestamp when the default prompt router was created.",
   "format": "date-time",
   "type": "string"
  },
  "Description": {
   "description": "The description of the default prompt router.",
   "maxLength": 200,
   "minLength": 1,
   "pattern": "^([0-9a-zA-Z:.][ _-]?)+$",
   "type": "string"
  },
  "FallbackModel": {
   "$ref": "#/definitions/PromptRouterTargetModel"
  },
  "Models": {
   "description": "A list of foundation models that the default prompt router can route requests to.",
   "insertionOrder": false,
   "items": {
    "$ref": "#/definitions/PromptRouterTargetModel"
   },
   "type": "array"
  },
  "PromptRouterArn": {
   "description": "The Amazon Resource Name (ARN) of the default prompt router.",
   "maxLength": 2048,
   "minLength": 1,
   "pattern": "^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:default-prompt-router/[a-zA-Z0-9-:.]+$",
   "type": "string"
  },
  "PromptRouterId": {
   "description": "The unique identifier of the default prompt router, extracted from the ARN.",
   "maxLength": 2048,
   "minLength": 1,
   "type": "string"
  },
  "PromptRouterName": {
   "description": "The name of the default prompt router.",
   "maxLength": 64,
   "minLength": 1,
   "pattern": "^([0-9a-zA-Z][ _-]?)+$",
   "type": "string"
  },
  "RoutingCriteria": {
   "$ref": "#/definitions/RoutingCriteria"
  },
  "Status": {
   "description": "The status of the default prompt router.",
   "enum": [
    "AVAILABLE"
   ],
   "type": "string"
  },
  "Type": {
   "description": "The type of the prompt router.",
   "enum": [
    "custom",
    "default"
   ],
   "type": "string"
  },
  "UpdatedAt": {
   "description": "The timestamp when the default prompt router was last updated.",
   "format": "date-time",
   "type": "string"
  }
 },
 "readOnlyProperties": [
  "/properties/PromptRouterArn",
  "/properties/PromptRouterId",
  "/properties/PromptRouterName",
  "/properties/Description",
  "/properties/RoutingCriteria",
  "/properties/FallbackModel",
  "/properties/Models",
  "/properties/Status",
  "/properties/Type",
  "/properties/CreatedAt",
  "/properties/UpdatedAt"
 ],
 "tagging": {
  "taggable": false
 },
 "typeName": "AWS::Bedrock::DefaultPromptRouter"
}
