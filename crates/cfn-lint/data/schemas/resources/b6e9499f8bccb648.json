{
 "additionalProperties": false,
 "createOnlyProperties": [
  "/properties/Recipients",
  "/properties/InstanceArn",
  "/properties/ExpiresAt",
  "/properties/Priority"
 ],
 "definitions": {
  "Arn": {
   "description": "The identifier of the instance.",
   "pattern": "^.{0,2048}$",
   "type": "string"
  },
  "InstanceArn": {
   "description": "The identifier of the instance.",
   "pattern": "^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*$",
   "type": "string"
  },
  "LocalizedString": {
   "description": "Localized notification content",
   "pattern": "^.{0,500}$",
   "type": "string"
  },
  "NotificationContent": {
   "additionalProperties": false,
   "description": "The content of a notification",
   "properties": {
    "DeDE": {
     "$ref": "#/definitions/LocalizedString"
    },
    "EnUS": {
     "$ref": "#/definitions/LocalizedString"
    },
    "EsES": {
     "$ref": "#/definitions/LocalizedString"
    },
    "FrFR": {
     "$ref": "#/definitions/LocalizedString"
    },
    "IdID": {
     "$ref": "#/definitions/LocalizedString"
    },
    "ItIT": {
     "$ref": "#/definitions/LocalizedString"
    },
    "JaJP": {
     "$ref": "#/definitions/LocalizedString"
    },
    "KoKR": {
     "$ref": "#/definitions/LocalizedString"
    },
    "PtBR": {
     "$ref": "#/definitions/LocalizedString"
    },
    "ZhCN": {
     "$ref": "#/definitions/LocalizedString"
    },
    "ZhTW": {
     "$ref": "#/definitions/LocalizedString"
    }
   },
   "type": "object"
  },
  "Priority": {
   "description": "The priority of notification. In the Amazon Connect console, when you create a notification, you are prompted to assign one of the following priorities: High (HIGH) or LOW (LOW)",
   "enum": [
    "HIGH",
    "LOW"
   ],
   "type": "string"
  },
  "RecipientList": {
   "description": "The recipients of the notification.",
   "insertionOrder": false,
   "items": {
    "$ref": "#/definitions/Arn"
   },
   "maxItems": 200,
   "minItems": 0,
   "type": "array",
   "uniqueItems": true
  },
  "Tag": {
   "additionalProperties": false,
   "description": "A key-value pair to associate with a resource.",
   "properties": {
    "Key": {
     "description": "The key name of the tag. You can specify a value that is 1 to 128 Unicode characters in length and cannot be prefixed with aws:. You can use any of the following characters: the set of Unicode letters, digits, whitespace, _, ., /, =, +, and -.",
     "maxLength": 128,
     "minLength": 1,
     "type": "string"
    },
    "Value": {
     "description": "The value for the tag. You can specify a value that is 0 to 256 Unicode characters in length and cannot be prefixed with aws:. You can use any of the following characters: the set of Unicode letters, digits, whitespace, _, ., /, =, +, and -.",
     "maxLength": 256,
     "minLength": 0,
     "type": "string"
    }
   },
   "required": [
    "Key",
    "Value"
   ],
   "type": "object"
  },
  "UserArn": {
   "description": "The identifier of the user.",
   "pattern": "^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*/agent/[-a-zA-Z0-9]*$",
   "type": "string"
  }
 },
 "description": "Resource Type definition for AWS::Connect::Notification",
 "handlers": {
  "create": {
   "permissions": [
    "connect:CreateNotification",
    "connect:TagResource"
   ]
  },
  "delete": {
   "permissions": [
    "connect:DeleteNotification",
    "connect:UntagResource"
   ]
  },
  "list": {
   "handlerSchema": {
    "properties": {
     "InstanceArn": {
      "$ref": "resource-schema.json#/properties/InstanceArn"
     }
    },
    "required": [
     "InstanceArn"
    ]
   },
   "permissions": [
    "connect:ListNotifications"
   ]
  },
  "read": {
   "permissions": [
    "connect:DescribeNotification"
   ]
  },
  "update": {
   "permissions": [
    "connect:UpdateNotificationContent",
    "connect:TagResource",
    "connect:UntagResource"
   ]
  }
 },
 "primaryIdentifier": [
  "/properties/Arn"
 ],
 "properties": {
  "Arn": {
   "description": "The Amazon Resource Name (ARN) for the notification.",
   "pattern": "^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*/notification/[-a-zA-Z0-9]*$",
   "type": "string"
  },
  "Content": {
   "$ref": "#/definitions/NotificationContent",
   "description": "The content of the notification."
  },
  "CreatedAt": {
   "description": "The time a notification was created",
   "pattern": "^[0-9]+$",
   "type": "string"
  },
  "ExpiresAt": {
   "description": "The time a notification will expire",
   "pattern": "^[0-9]+$",
   "type": "string"
  },
  "Id": {
   "description": "The identifier of the notification.",
   "pattern": "^.{0,256}$",
   "type": "string"
  },
  "InstanceArn": {
   "description": "The identifier of the Amazon Connect instance.",
   "pattern": "^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*$",
   "type": "string"
  },
  "Priority": {
   "$ref": "#/definitions/Priority",
   "description": "The priority of the notification."
  },
  "Recipients": {
   "$ref": "#/definitions/RecipientList",
   "description": "The recipients of the notification."
  },
  "Tags": {
   "description": "One or more tags.",
   "insertionOrder": false,
   "items": {
    "$ref": "#/definitions/Tag"
   },
   "maxItems": 200,
   "type": "array",
   "uniqueItems": true
  }
 },
 "readOnlyProperties": [
  "/properties/Arn",
  "/properties/Id",
  "/properties/CreatedAt"
 ],
 "required": [
  "InstanceArn",
  "Content"
 ],
 "sourceUrl": "https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-connect",
 "tagging": {
  "cloudFormationSystemTags": true,
  "permissions": [
   "connect:TagResource",
   "connect:UntagResource"
  ],
  "tagOnCreate": true,
  "tagProperty": "/properties/Tags",
  "tagUpdatable": true,
  "taggable": true
 },
 "typeName": "AWS::Connect::Notification"
}
